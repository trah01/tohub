name: tohub

services:
  tohub:
    build: .
    container_name: tohub
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      ADDR: ":8080"
      PUBLIC_BASE_URL: "http://localhost:8080"
